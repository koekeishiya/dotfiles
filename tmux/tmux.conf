set-option -g default-shell /bin/zsh
set-option -g default-terminal "screen-256color"

set-option -g status-position bottom
set -g status-justify right
set -g status-interval 2
# set -g status-right '%H:%M'
set -g status-right ''
set -g status-left ''
set -g status-fg white
set -g status-bg default

set -g renumber-windows on
set -g automatic-rename on
set-option -g -q mouse on

set -g default-command "reattach-to-user-namespace -l zsh"

setw -g mode-keys vi

unbind v
bind v copy-mode

set -g base-index 1
setw -g aggressive-resize on
set -sg escape-time 0

bind r source-file ~/.tmux.conf

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

bind x kill-pane
bind t set status

bind-key -n S-left prev
bind-key -n S-right next
bind-key -n C-left swap-window -t -1
bind-key -n C-right swap-window -t +1

set -g pane-active-border-fg colour10
# set -g window-status-format "#[fg=white,bg=default] #I #[fg=#7f7f7f,bg=default] #W "
# set -g window-status-current-format "#[fg=black,bg=white] #I #[fg=white,bg=default] #W "

#run-shell "powerline-daemon -q"
#source "/usr/local/lib/python3.5/site-packages/powerline/bindings/tmux/powerline.conf"

setw -g window-status-format "#[bg=default,fg=colour8,noreverse]▉▊▋▌ #W "
setw -g window-status-current-format "#[bg=default,fg=colour7,noreverse]▉▊▋▌ #W "
